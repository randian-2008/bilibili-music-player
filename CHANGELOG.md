# 更新日志

本文件记录项目自首次公开发布以来的重要用户可见变化。公开前的内部迭代保留在 Git 提交历史中，不再作为正式版本逐项列出。

## [2.8.6] - 2026-09-18

### 修复

- 列表编辑按稳定条目 ID 执行，防止切换列表、异步重排后误改或误删；拒绝同列表批量移动。
- 停止、切歌及系统媒体键可取消旧匹配的自动播放，播放状态字段统一串行保存。
- 修复所有媒体地址失败后自恢复停止推进的问题，分开匹配、解析和媒体起播的时间预算。
- 手动匹配新增静音媒体起播验证，验证成功后才保存来源；不可用候选继续有界搜索，网络异常保留原来源。
- 无源手动条目重命名同步搜索目标；备份恢复与中断任务不会留下永久禁用的搜索按钮。
- 热榜导入超时重试复用同一次请求，避免重复创建播放列表。
- 面板位置在标签页间同步，未实际拖动或调整尺寸时不写回旧位置；保留旧坐标读取兼容。
- 消息桥校验真实面板窗口，拒绝无关 iframe 的播放控制。
- 自定义重命名规则使用有工作量上限的常用正则子集，避免阻塞后台；现有默认关闭示例仍可启用。
- 更新成功后的备份清理失败不再触发回滚；拒绝源码覆盖、危险打包路径和缺少运行文件的发布包。

### 维护

- 增加跨模块异步、界面事件和 Windows 升级故障回归测试。
- 补充站点权限、匿名访问、用户规则语法与升级兼容说明，保留 Chrome 109–115 的 offscreen 检测分支。

## [2.8.5] - 2026-09-14

### 修复

- 面板位置改为按可移动区域的横纵比例保存，在窗口缩放、分屏或切换显示尺寸后仍保持用户选择的相对位置。
- 窗口尺寸变化时只临时调整面板位置和显示尺寸，不再覆盖用户保存的位置与期望尺寸。
- 兼容旧版绝对坐标数据，并在读取后自动迁移到新版位置格式。

## [2.8.4] - 2026-08-26

### 改进

- B站搜索接口暂时返回 HTTP 412 时，自动按退避间隔有限重试；热榜匹配和替代源匹配不再要求用户反复点击匹配按钮。
- 搜索阶段的 412 重试与候选可播放性验证分层处理，持续被风控拒绝时仍会有界结束并保留手动重试入口。
- 热榜导入广播所需的本地存储读取改为并行，减少榜单请求完成后的界面等待。

### 修复

- B站公开接口使用匿名请求，不再通过全局网络规则修改 `api.bilibili.com` 的网页请求头，避免干扰 B站网页登录。

## [2.8.3] - 2026-08-25

### 新增

- 播放列表工具栏增加“＋”手动添加入口，可输入名称或搜索关键词创建待匹配条目。
- 手动条目支持按需搜索 B 站音源；匹配成功后保留用户输入的显示标题，并记录来源历史。
- 手动条目支持 JSON 完整备份与恢复，未匹配条目不会被自动播放流程联网。

### 改进

- 工具栏统一为“播放列表、添加条目、热榜、更多操作”；新建播放列表移入“更多操作”菜单。
- 手动匹配使用现有候选评分逻辑和更宽的搜索范围，匹配失败时可再次尝试。
- 统一按钮的居中布局和图标描边，避免不同字体或主题下出现视觉偏移。

## [2.8.2] - 2026-08-12

### 新增

- 自动匹配得到的条目可在时长区域悬浮后重新匹配音源；已确认失效的条目会持续显示该入口。
- 为自动匹配条目记录固定匹配目标和完整来源历史，重新匹配时不会再次选择当前或曾用过的视频。
- JSON 导出升级为完整播放列表备份，可恢复音源、匹配目标、匹配历史和待匹配状态等内部条目信息。

### 改进

- 热榜首次匹配、热榜重新匹配和失效源替换共用统一的候选评分逻辑，并保留条目的固定显示标题。
- TXT 和 Markdown 继续只导出标题、UP 主、时长与链接等显式信息，适合阅读和分享。
- JSON 导入兼容旧版基础格式，恢复时重新生成播放列表和条目 ID，并过滤无效条目和危险对象键。
- 自动化测试增至 260 项，覆盖匹配历史、重复来源排除、完整备份恢复和导出信息边界。

## [2.8.1] - 2026-08-12

### 新增

- 播放列表中的原视频被明确删除或失效后，显示失效封面和替代源匹配按钮。
- 用户可主动搜索并验证替代视频；匹配成功后更新播放来源和原页面链接，同时保留播放列表中的显示标题。

### 改进

- 替代源采用两阶段匹配：先排除与原条目意图冲突的翻唱、伴奏、AI 改编、现场、教程和片段等内容，再按标题、时长、歌手、搜索排名及热度排序。
- 自动播放会跳过已确认失效的条目；整张播放列表均失效时有界停止，避免反复请求。
- 仅将 B站明确返回永久失效状态的条目标记为失效，网络波动、风控或临时取源失败不会触发换源流程。
- 自动化测试增至 249 项，覆盖失效识别、候选过滤、替代源验证、原子替换和播放列表跳过逻辑。

## [2.8.0] - 2026-08-11

### 新增

- 增加热榜导入窗口，支持选择音乐平台、榜单分类、具体榜单、导入数量和播放列表名称。
- 首批支持 Apple Music、QQ 音乐和网易云音乐，包含综合、地区、风格、ACG、国风等榜单分类。
- 导入后创建独立播放列表，并按播放进度渐进匹配 B站视频；手动点击待匹配条目时优先处理该条目。

### 改进

- 第三方榜单统一只保存排名、歌曲名和歌手名；BVID、CID、封面、时长和 UP 主均来自 B站匹配结果。
- 热榜条目显示名固定为“歌曲名 - 歌手”，匹配和首次播放补齐 CID 时均不会被 B站原标题覆盖。
- 支持导入前 10、25 或 50 个条目；扩大榜单选择窗口并精简内部标题区域。
- 随机播放按实际选中的条目优先匹配，失败后跳过；整轮失败时有界停止，不重复自动请求失败条目。
- 批量移动和复制菜单提升为视口级浮层，分别按左、右按钮对齐，并支持按可用空间翻转和独立滚动。
- 最小发布脚本补齐热榜运行时文件，自动化测试增至 236 项。

### 修复

- 修复随机播放首次建立序列或随机循环换轮时，首个热榜条目匹配失败会阻断后续播放的问题。
- 修复批量移动和复制菜单被歌曲列表遮挡、越界后无法操作的问题。
- 修复热榜条目匹配成功后，在首次播放补齐 CID 时再次同步为 B站源视频标题的问题。

## [2.7.0] - 2026-08-09

### 新增

- 增加 Windows `update.bat` 更新入口，用户可在扩展目录中双击检查并部署最新版。

### 改进

- 更新器从官方 GitHub Release 下载完整发布包，校验 SHA-256 后再替换文件。
- 更新过程使用暂存目录和旧版本备份，部署失败时尝试恢复旧版。
- 更新时保留用户自定义的 `src/rename/rules.json`；浏览器本地播放列表和界面设置继续由 `chrome.storage.local` 管理。
- 发布脚本同时生成版本化 ZIP 和对应的 `.sha256` 校验文件，并补充更新文档。
- 修复 Windows 批处理传递带末尾反斜杠的安装路径时可能导致 PowerShell 路径解析失败的问题。

## [2.6.4] - 2026-08-09

### 改进

- 在“智能重命名”旁增加简洁的信息提示，说明功能准确性边界及 `rules.json` 自定义入口。
- 信息提示支持鼠标悬浮和键盘聚焦，不改变复选框原有操作。

## [2.6.3] - 2026-08-09

### 新增

- 点击播放器封面或标题可切换到当前条目所属播放列表，并自动滚动和高亮该条目。

### 改进

- 将扩展运行时代码整理到 `src/`，图标移至 `assets/`，用户指南移至 `docs/`，并同步更新 Manifest、测试和最小发布脚本。
- 将界面与文档中的“歌单”统一改为适用范围更广的“播放列表”，条目计数使用中性表述。
- 合集导入成功后移除无意义的“取消”，完成态仅保留“确认”；再次导入或失败后会恢复完整操作按钮。
- 自动化测试增至 210 项，覆盖当前条目定位、导入完成态、目录路径和智能重命名等行为。

## [2.6.2] - 2026-08-08

### 新增

- 合集导入确认框增加可选的“智能重命名”和统一前缀。
- 智能重命名按分段、批量公共前后缀和长文案过滤缩短标题；用户可通过 `src/rename/rules.json` 启用静态正则替换规则。

### 改进

- 默认重命名逻辑完全内置于 JavaScript；规则文件为空、加载失败或含非法规则时仍可正常导入。
- 重命名结果只写入最终标题，不保存原始标题，也不改变 `bvid + cid` 去重逻辑。
- 增加重命名模块测试，并将其纳入语法检查、完整测试和发布包。

## [2.6.1] - 2026-08-08

### 新增

- 在 B站 UGC 合集、列表和普通多 P 视频页面提供“全部加入”，可导入当前播放列表或创建新播放列表。
- 导入前显示合集标题、条目数量和目标播放列表；导入后报告新增及重复条目数量。

### 改进

- 合集完整条目仅在后台解析和缓存，页面端只接收摘要，减少大型合集的跨上下文数据传输。
- 按分区、剧集和分 P 顺序展开条目，并按 `bvid + cid` 批内及目标播放列表去重。
- 增加同一 BVID 请求合并、五分钟缓存、SPA 切页迟到响应隔离，以及网络超时和存储配额提示。

### 修复

- 修复确认导入时消息路由字段被导入目标覆盖，导致后台端口提前关闭、合集无法写入的问题。
- 修正合集条目命名：分区标题不再进入歌曲名称，非嵌套合集和普通多 P 仅使用分 P 名称。
- 仅对合集内嵌多 P 使用“视频标题 · 分 P 名称”前缀，并避免标题与分 P 名称相同时重复显示。
- 自动化测试增至 183 项，覆盖合集解析、嵌套多 P、批量导入、普通视频识别、命名规则和前端异步竞态。

## [2.5.3] - 2026-08-06

### 修复

- 修复面板高度较小时，播放列表操作菜单向上展开后被播放器遮挡且无法交互的问题。
- 将播放列表操作菜单提升为视口级浮层，避免受播放列表层叠上下文及玻璃主题工具栏滤镜影响。
- 增加菜单 DOM 层级回归检查，自动化测试增至 164 项。

## [2.5.2] - 2026-08-05

### 修复

- 修复「僵尸按钮」问题：音频模块收到 Port 命令后立即 ACK；未 ACK 时主动断开陈旧连接，并以相同 `requestId` 经短消息通道重发。音频模块会对请求去重，避免双通道重复执行。
- 播放/切歌和快速控制采用分级响应预算；进度、音量和播放模式不再被慢速取源命令排队阻塞。
- 网络请求、媒体加载、Blob 读取和 `audio.play()` 均增加有界超时；B站接口对超时、限流和服务端错误进行一次有限重试。
- 新播放会取消旧的取源和媒体加载结果，避免快速切歌时旧音源晚到覆盖当前歌曲。
- 播放途中遇到 `audio.error` 或长期卡流时会重新解析音源并从断点有限恢复；持续失败会停止播放并显示明确错误。短暂卡流自行恢复后不会触发多余重播。
- 音频模块上下文损坏或双通道无响应时，在本条命令内有界重建一次，并以冷却保护避免反复重建。
- 修复残缺曲目（无 cid/封面/标题）无法播放的问题：点播时自动解析补齐。
- 修复新建播放列表后点播索引被按默认播放列表解释的问题。
- 自动化测试增至 163 项，覆盖慢响应、请求去重、永久挂起、快速切歌取消和播放自恢复。

## [2.5.1] - 2026-08-04

### 首次公开发布

- 支持将 B站视频及当前分 P 添加至自定义播放列表。
- 使用 Manifest V3 Offscreen Document 实现跨页面持续播放，并提供多音源容错与断点恢复。
- 支持多播放列表、拖拽排序、批量移动/复制/删除，以及 JSON 导入和 TXT/Markdown/JSON 导出。
- 提供播放进度、音量、五种播放模式和面板状态持久化。
- 补充最终用户指南、维护者架构文档、贡献指南、安全政策及 GitHub Issue/PR 模板。
- 建立包含 146 项断言的自动化测试与 GitHub Actions CI。
- 采用 GNU GPL v3.0 或更高版本开源。

[2.8.6]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.8.6
[2.8.5]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.8.5
[2.8.4]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.8.4
[2.8.3]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.8.3
[2.8.2]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.8.2
[2.8.1]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.8.1
[2.8.0]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.8.0
[2.7.0]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.7.0
[2.6.4]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.6.4
[2.6.3]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.6.3
[2.6.2]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.6.2
[2.6.1]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.6.1
[2.5.3]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.5.3
[2.5.2]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.5.2
[2.5.1]: https://github.com/randian-2008/bilibili-music-player/releases/tag/v2.5.1
